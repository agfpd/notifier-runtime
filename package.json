{
  "name": "@agfpd/notifier-runtime",
  "version": "0.3.1",
  "description": "Notifier runtime-router for IAP peers — time (cron/heartbeat) and event (watcher) primitives.",
  "license": "Apache-2.0",
  "type": "module",
  "bin": {
    "notifier-runtime": "bin/notifier-runtime"
  },
  "files": [
    "bin/",
    "docs/",
    "src/",
    "tsconfig.json"
  ],
  "scripts": {
    "start": "bun src/cli.ts run",
    "self-install": "bun src/cli.ts self-install",
    "self-config": "bun src/cli.ts self-config",
    "doctor": "bun src/cli.ts doctor --json",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "latest"
  }
}
